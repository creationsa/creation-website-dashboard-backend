apps:
  - name: DarfQueue
    script: artisan
    exec_mode: fork
    interpreter: php
    instances: 1
    args:
      - queue:work
      - "--queue=default,schedule_event"
      - --tries=5
      - --sleep=1

